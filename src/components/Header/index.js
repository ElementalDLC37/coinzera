import styled from "styled-components"
import { styleHeader } from "./styles"

export function Header() {
  return (
    <header className={styled.styleHeader}>
        <h1>Coinzera</h1>
    </header>
  )
}